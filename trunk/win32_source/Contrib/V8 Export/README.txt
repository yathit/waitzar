This file is added to the V8 build; it allows us to easily reference 
the V8 javascript engine using only a single method (for converting strings).

We may clean up the process later; for now, it's easier to debug the MinGW 
to MSVC connection through a single point of entry.
